spring.application.name=workout-app

# Databasinställningar
spring.datasource.url=jdbc:sqlite:workout.db
spring.datasource.driver-class-name=org.sqlite.JDBC
spring.datasource.username=
spring.datasource.password=

# Använd H2 dialect för SQLite-kompatibilitet
spring.jpa.database-platform=org.hibernate.community.dialect.SQLiteDialect

# VIKTIGT: Använd 'none' för att bevara befintlig databas
spring.jpa.hibernate.ddl-auto=validate

# Förhindra att JPA försöker skapa schema
spring.jpa.generate-ddl=false

# Debugging (kan tas bort senare)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Logging
logging.file.name=/var/log/myapp/app.log

# JWT Configuration
# app.jwtSecret=r6F50xlRJB9RBKUJiFbyLOOh3m2njzqyZCSUrYittiz8F/p1hGXjjw5JrmjeLJ/6awAtdvWvKvCSUFUQl7k27Q==
# app.jwtExpirationMs=1000000